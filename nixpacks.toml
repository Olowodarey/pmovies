[phases.setup]
nixPkgs = ['nodejs_22']

[phases.install]
cmds = ['pnpm install --frozen-lockfile']

[phases.build]
cmds = ['pnpm --filter backend run prisma:generate', 'pnpm --filter backend run build']

[start]
cmd = 'cd backend && pnpm run prisma:migrate && pnpm run start:prod'

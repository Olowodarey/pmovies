[build]
builder = "nixpacks"
watchPatterns = ["backend/**"]

[deploy]
startCommand = "cd backend && pnpm run prisma:migrate && pnpm run start:prod"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[build]
builder = "nixpacks"
buildCommand = "cd backend && pnpm install && pnpm prisma generate && pnpm build"

[deploy]
startCommand = "cd backend && pnpm prisma migrate deploy && pnpm start:prod"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

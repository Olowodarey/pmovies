{
  "name": "pmovies-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "build": "cd backend && pnpm run prisma:generate && pnpm run build"
  }
}
